import React, { FC } from 'react'

import ApolloConnector from '~/components/ApolloConnector'

const App: FC = () => <h1>App</h1>

// const onSuccess: FC<any> = (props: any) => <App {...props} />
// const Connector: FC = () => <ApolloConnector query={{}} onSuccess={onSuccess} />

export default App
